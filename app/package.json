{
  "name": "thincart-web",
  "version": "0.1.0",
  "private": true,
  "description": "Tests for the served web app (app/index.html) — the layer the phones actually run",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
