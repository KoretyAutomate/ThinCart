{
  "name": "thincart-app",
  "version": "0.1.0",
  "private": true,
  "description": "ThinCart — Android app (Capacitor shell around the tailnet-served PWA)",
  "scripts": {
    "sync": "cap sync android",
    "build:apk": "cd android && ./gradlew assembleDebug",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
